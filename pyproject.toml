[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "jianying-sdk"
version = "0.2.0"
description = "Python SDK for Jianying draft creation, editing, encryption, decryption, and export workflows."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "pycryptodome>=3.20",
]
authors = [
  { name = "OpenAI Codex" }
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
